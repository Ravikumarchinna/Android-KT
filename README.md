# Android-KT
Android-KT-Sessions
1. Make sure the .gitignore is configured correctly so that the files are not pushed
**Manifest XML**:
It is an app manifest file in which we can configure all application settings such as camera access, library access, network access, app icon setup, activity launch point, and more. 
**Activity**:
1. It is a view; it handles the design part and the connection of   elements. 
**Resource**:
1. Images and colors will be loaded from the resources.
**Gradle**:
1. It handles the library, framework version, and SDK parts.

